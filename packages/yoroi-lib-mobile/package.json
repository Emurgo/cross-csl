{
  "name": "yoroi-lib-mobile",
  "version": "0.0.1-alpha.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf ./dist",
    "rebuild": "yarn clean && yarn build",
    "pack:dry": "yarn rebuild && npm pack --dry-run",
    "pack": "yarn rebuild && npm pack"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@emurgo/react-native-haskell-shelley": "2.0.0"
  }
}

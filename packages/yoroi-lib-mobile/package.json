{
  "name": "@emurgo/yoroi-lib-mobile",
  "version": "0.0.1-alpha.0",
  "description": "",
  "scripts": {
    "build": "yarn install && node_modules/typescript/bin/tsc",
    "clean": "rm -rf ./dist",
    "gen-flow-defs": "node ../../generate-flow.js",
    "rebuild": "yarn clean && yarn build && yarn gen-flow-defs",
    "pack-tar:dry": "yarn rebuild && npm pack --dry-run",
    "pack-tar": "yarn rebuild && npm pack"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@emurgo/react-native-haskell-shelley": "^3.1.2",
    "@emurgo/yoroi-lib-core": "file:../yoroi-lib-core"
  },
  "devDependencies": {
    "typescript": "^4.4.4"
  }
}

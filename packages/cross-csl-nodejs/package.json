{
  "name": "@emurgo/cross-csl-nodejs",
  "version": "2.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "mocha",
    "test:watch": "mocha --watch",
    "build": "yarn install && node_modules/typescript/bin/tsc",
    "clean": "rm -rf ./dist",
    "gen-flow-defs": "node ../../generate-flow.js",
    "rebuild": "yarn clean && yarn build && yarn gen-flow-defs",
    "pack-tar:dry": "yarn rebuild && npm pack --dry-run",
    "pack-tar": "yarn rebuild && npm pack",
    "publish:beta": "npm publish --tag beta --access public",
    "publish:prod": "npm publish --access public",
    "release": "yarn rebuild && npm publish --scope=emurgo --access public"
  },
  "files": [
    "dist/**"
  ],
  "author": "EMURGO",
  "license": "MIT",
  "dependencies": {
    "@emurgo/cardano-serialization-lib-nodejs": "11.1.0",
    "@emurgo/cross-csl-core": "2.1.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.56",
    "chai": "^4.3.4",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}

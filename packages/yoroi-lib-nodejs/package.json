{
  "name": "@emurgo/yoroi-lib-nodejs",
  "version": "0.8.5-alpha.47",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "mocha",
    "test:watch": "mocha --watch",
    "build": "yarn install && node_modules/typescript/bin/tsc",
    "clean": "rm -rf ./dist",
    "gen-flow-defs": "node ../../generate-flow.js",
    "rebuild": "yarn clean && yarn build && yarn gen-flow-defs",
    "pack-tar:dry": "yarn rebuild && npm pack --dry-run",
    "pack-tar": "yarn rebuild && npm pack",
    "publish:beta": "npm publish --tag beta --access public",
    "publish:prod": "npm publish --access public",
    "release": "yarn rebuild && npm publish --scope=emurgo"
  },
  "files": [
    "dist/**"
  ],
  "author": "EMURGO",
  "license": "MIT",
  "dependencies": {
    "@emurgo/cardano-serialization-lib-nodejs": "9.1.4",
    "@emurgo/yoroi-lib-core": "^0.8.5-alpha.47"
  },
  "devDependencies": {
    "@types/chai": "^4.2.21",
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.10.2",
    "@types/sinon": "^10.0.2",
    "chai": "^4.3.4",
    "mocha": "^9.1.1",
    "prettier": "^2.4.1",
    "sinon": "^11.1.2",
    "ts-mocha": "^8.0.0",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
